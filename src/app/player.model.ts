export class Player {
  // public $key: string;
constructor(public name: string, public age: string, public birthplace: string, public team: string, public position: string, public jerseyNumber: string, public image: string, public goalsThisSeason: number) {}
}
